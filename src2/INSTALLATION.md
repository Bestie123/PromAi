# 📦 Установка PromAi Inspector Pro v3.1

## ⚡ Быстрая установка (30 секунд)

### Шаг 1: Откройте Chrome Extensions
```
chrome://extensions/
```

### Шаг 2: Включите режим разработчика
Переключатель в правом верхнем углу: **Developer mode** → ON

### Шаг 3: Загрузите расширение
1. Нажмите **"Load unpacked"** (Загрузить распакованное)
2. Выберите папку `src2`
3. Готово! ✅

---

## 🎯 Первое использование

### 1. Откройте любой сайт
Например:
- https://youtube.com
- https://github.com
- https://google.com
- Любой другой сайт!

### 2. Нажмите на иконку расширения
В панели инструментов Chrome (справа от адресной строки)

### 3. Нажмите кнопку
**"Make Site PromAi-Ready"**

### 4. Проверьте результат
Откройте DevTools (F12) → Elements → найдите любой элемент:
```html
<div data-component-id="COMP_10000">...</div>
<button data-component-id="COMP_10001">Click</button>
```

---

## 🔍 Проверка установки

### Через Chrome Extensions:
1. Откройте `chrome://extensions/`
2. Найдите "PromAi Inspector Pro v3.1"
3. Статус должен быть: **Enabled** ✅

### Через Console:
```javascript
// Откройте любой сайт
// Нажмите F12 → Console
// Выполните:
document.querySelectorAll('[data-component-id]').length
// Должно вернуть 0 (до обработки) или число (после)
```

---

## 🛠️ Создание иконок (опционально)

Расширение работает без иконок, но для красоты:

### Вариант 1: Онлайн генератор
1. Перейдите на https://www.favicon-generator.org/
2. Загрузите любое изображение
3. Скачайте иконки 16x16, 48x48, 128x128
4. Переименуйте в `icon16.png`, `icon48.png`, `icon128.png`
5. Поместите в папку `src2/icons/`

### Вариант 2: Простые цветные квадраты
Создайте в любом редакторе (Paint, Photoshop):
- 16x16 пикселей - фиолетовый квадрат → `icon16.png`
- 48x48 пикселей - фиолетовый квадрат → `icon48.png`
- 128x128 пикселей - фиолетовый квадрат → `icon128.png`

### Вариант 3: Использовать emoji
Создайте HTML файл:
```html
<!DOCTYPE html>
<html>
<body style="margin:0; background:#667eea;">
  <div style="font-size:100px; text-align:center;">🚀</div>
</body>
</html>
```
Откройте в браузере → Сделайте скриншот → Обрежьте до нужных размеров

---

## 🔧 Настройка (опционально)

### Изменить начальный счётчик:
Откройте `src2/popup.js`, строка 20:
```javascript
let counter = parseInt(localStorage.getItem('promai_comp_counter') || '10000');
// Измените '10000' на нужное значение, например '50000'
```

### Изменить префикс ID:
Откройте `src2/popup.js`, строка 28:
```javascript
el.setAttribute('data-component-id', `COMP_${counter++}`);
// Измените 'COMP_' на нужный префикс, например 'ELEMENT_'
```

### Исключить определённые элементы:
Откройте `src2/popup.js`, строка 24:
```javascript
const elements = document.querySelectorAll('*:not(script):not(style):not(noscript)');
// Добавьте :not(header):not(footer) и т.д.
```

---

## 🚀 Тестирование

### Тест 1: Простая страница
```html
<!DOCTYPE html>
<html>
<body>
  <div>Test 1</div>
  <button>Test 2</button>
</body>
</html>
```
Сохраните как `test.html` → Откройте → Запустите расширение → Проверьте DevTools

### Тест 2: Страница с iframe
```html
<!DOCTYPE html>
<html>
<body>
  <div>Main page</div>
  <iframe src="https://example.com"></iframe>
</body>
</html>
```
Расширение должно обработать и основную страницу, и iframe

### Тест 3: YouTube
1. Откройте https://youtube.com
2. Запустите расширение
3. Проверьте DevTools → Elements
4. Все видимые элементы должны иметь `data-component-id`

---

## ❓ Troubleshooting

### Проблема: Расширение не появляется в панели
**Решение:**
1. Откройте `chrome://extensions/`
2. Убедитесь что расширение **Enabled**
3. Нажмите кнопку "Reload" (🔄) на карточке расширения

### Проблема: Кнопка не работает
**Решение:**
1. Откройте DevTools (F12) → Console
2. Проверьте наличие ошибок
3. Убедитесь что сайт не блокирует скрипты
4. Попробуйте другой сайт

### Проблема: Не обрабатываются iframe
**Решение:**
1. Cross-origin iframe требуют content script
2. Убедитесь что в `manifest.json` есть:
```json
"content_scripts": [{
  "all_frames": true
}]
```
3. Перезагрузите расширение

### Проблема: Счётчик не сохраняется
**Решение:**
1. Проверьте localStorage:
```javascript
localStorage.getItem('promai_comp_counter')
```
2. Убедитесь что сайт разрешает localStorage
3. Попробуйте очистить localStorage:
```javascript
localStorage.removeItem('promai_comp_counter')
```

---

## 🔄 Обновление расширения

### После изменения кода:
1. Откройте `chrome://extensions/`
2. Найдите "PromAi Inspector Pro v3.1"
3. Нажмите кнопку "Reload" (🔄)
4. Готово!

### После обновления manifest.json:
1. Удалите расширение
2. Установите заново (Load unpacked)

---

## 📊 Проверка работы

### Команды для Console:

```javascript
// Посмотреть все обработанные элементы
document.querySelectorAll('[data-component-id]').length

// Посмотреть первые 10 элементов
Array.from(document.querySelectorAll('[data-component-id]')).slice(0, 10)

// Посмотреть конкретный элемент
document.querySelector('[data-component-id="COMP_10000"]')

// Посмотреть текущий счётчик
localStorage.getItem('promai_comp_counter')

// Сбросить счётчик
localStorage.setItem('promai_comp_counter', '10000')

// Посмотреть все модули
document.querySelectorAll('[data-module-id]')
```

---

## 🎉 Готово!

Расширение установлено и готово к использованию!

### Следующие шаги:
1. ✅ Протестируйте на разных сайтах
2. ✅ Настройте под свои нужды
3. ✅ Изучите варианты расширения (см. VARIANTS.md)
4. ✅ Добавьте иконки (опционально)

---

## 📚 Дополнительная документация

- [README.md](README.md) - Полное описание
- [VARIANTS.md](VARIANTS.md) - Варианты реализации
- [manifest.json](manifest.json) - Конфигурация расширения

---

**Приятного использования! 🚀**
