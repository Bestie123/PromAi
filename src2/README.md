# 🚀 PromAi Inspector Pro v3.2

**Chrome Extension для автоматической PromAi-совместимости любого сайта**

## ✨ Возможности

### 🔧 Автоматическая обработка
- ✅ **Одна кнопка** - "Make Site PromAi-Ready"
- ✅ **Автоматические COMP_ID** - `data-component-id="COMP_10000"`, `COMP_10001"`, ...
- ✅ **Автоматические FUNC_ID** - `__funcId` к JavaScript функциям
- ✅ **Модульные ID** - `data-module-id="M_AUTO_1.0"`
- ✅ **Работа с iframe** - Включая cross-origin (YouTube, Google Docs)
- ✅ **Умная обработка** - Пропуск существующих ID

### 🔍 Inspector (Полный функционал из src)
- ✅ **Визуальная подсветка** - Красная/зелёная рамка при наведении
- ✅ **Отображение ID** - MODULE_ID, COMPONENT_ID, FUNCTION_ID
- ✅ **Закрепление** - Клик для закрепления элемента
- ✅ **Копирование** - 📋 кнопка для копирования ID
- ✅ **3 вкладки** - Элемент / Трассировка / Граф
- ✅ **Живая трассировка** - Обновление каждую секунду
- ✅ **Цветовая кодировка** - 🟢 <5ms, 🟡 5-10ms, 🔴 >10ms
- ✅ **Статистика** - Всего вызовов, среднее время
- ✅ **Граф зависимостей** - Визуализация модулей
- ✅ **Проверка циклов** - DFS алгоритм
- ✅ **Экспорт отчётов** - JSON / Markdown / HTML
- ✅ **Mermaid диаграммы** - Генерация графов
- ✅ **Горячая клавиша** - F2
- ✅ **Кнопка активации** - В popup расширения

## 📦 Установка

### Вариант 1: Из исходников (для разработки)

1. Откройте Chrome и перейдите в `chrome://extensions/`
2. Включите "Режим разработчика" (Developer mode) в правом верхнем углу
3. Нажмите "Загрузить распакованное расширение" (Load unpacked)
4. Выберите папку `src2`
5. Готово! Расширение установлено

### Вариант 2: Из .crx файла (для продакшена)

1. Упакуйте расширение в Chrome Extensions
2. Установите .crx файл

## 🎯 Использование

1. **Откройте любой сайт** (например, youtube.com, google.com, github.com)
2. **Нажмите на иконку расширения** в панели инструментов Chrome
3. **Нажмите кнопку** "Make Site PromAi-Ready"
4. **Активируйте Inspector** - нажмите `F2` или кнопку "🔍 Activate Inspector"
5. **Готово!** Наведите мышь на элементы для просмотра ID

### Пример результата:

**До:**
```html
<div class="header">
  <button>Click me</button>
</div>
```

**После:**
```html
<div class="header" data-component-id="COMP_10000">
  <button data-component-id="COMP_10001">Click me</button>
</div>
<script data-module-id="M_AUTO_1.0">
  // FUNC_calculateTotal_001 (runtime)
  function calculateTotal(items) { ... }
  calculateTotal.__funcId = 'FUNC_calculateTotal_001'
</script>
```

## 🔍 Проверка работы

### Через DevTools:

1. Откройте DevTools (F12)
2. Перейдите в Elements
3. Найдите любой элемент
4. Проверьте наличие `data-component-id="COMP_XXXXX"`

### Через Console:

```javascript
// Посмотреть все обработанные элементы
document.querySelectorAll('[data-component-id]').length

// Посмотреть конкретный элемент
document.querySelector('[data-component-id="COMP_10000"]')

// Посмотреть FUNC_ID функции
console.log(calculateTotal.__funcId) // "FUNC_calculateTotal_001"

// Посмотреть счётчики
localStorage.getItem('promai_comp_counter') // Элементы
localStorage.getItem('promai_func_counter')  // Функции
```

## 🎨 Варианты реализации

### Вариант 1: Базовый (текущий)
- ✅ Простая реализация
- ✅ Работает с основным DOM
- ✅ Обрабатывает same-origin iframe
- ✅ postMessage для cross-origin iframe
- ⚠️ Cross-origin iframe требуют content script на целевом сайте

### Вариант 2: Расширенный (опционально)
- ✅ Все из Варианта 1
- ✅ MutationObserver для динамических элементов
- ✅ Автоматическая обработка новых элементов
- ✅ Настройки префикса ID
- ✅ Экспорт/импорт конфигурации

### Вариант 3: Pro (опционально)
- ✅ Все из Варианта 2
- ✅ Визуальный Inspector (как в PromAi)
- ✅ Подсветка элементов при наведении
- ✅ Копирование ID в буфер обмена
- ✅ Статистика обработки
- ✅ Фильтры по типу элементов

## 🛠️ Технические детали

### Архитектура:

```
PromAi Inspector Pro v3.1
│
├── manifest.json          # Manifest V3 конфигурация
├── popup.html             # UI расширения
├── popup.js               # Логика popup
├── content.js             # Content script для iframe
└── icons/                 # Иконки расширения
```

### Алгоритм работы:

1. **Popup.js** → Инжектирует функцию в активную вкладку
2. **processPage()** → Обрабатывает основной DOM
3. **Content.js** → Слушает postMessage для iframe
4. **localStorage** → Сохраняет счётчик между сессиями

### Обработка iframe:

```javascript
// Same-origin iframe
if (iframe.contentWindow.document) {
  processFrameDocument(iframe.contentWindow.document);
}

// Cross-origin iframe
iframe.contentWindow.postMessage({
  type: 'PROMAI_PROCESS',
  startCounter: counter
}, '*');
```

## 📊 Статистика

После обработки показывается:
- ✅ Количество обработанных элементов (COMP_XXXXX)
- ✅ Количество найденных функций (FUNC_name_XXX)
- ✅ Количество обработанных фреймов
- ✅ Статус выполнения

**Пример:**
```
✅ Success! 1,234 elements, 56 functions across 3 frame(s)
```

## ⚠️ Ограничения

### Cross-origin iframe:
- Требуют content script на целевом домене
- Работают через postMessage
- Могут иметь задержку обработки

### Решение:
Расширение автоматически инжектирует content.js во все фреймы через `all_frames: true` в manifest.json

## 🔧 Настройка

### Изменить начальный счётчик:

```javascript
// В popup.js, строка 20
let counter = parseInt(localStorage.getItem('promai_comp_counter') || '10000');
// Измените '10000' на нужное значение
```

### Изменить префикс ID:

```javascript
// В popup.js, строка 28
el.setAttribute('data-component-id', `COMP_${counter++}`);
// Измените 'COMP_' на нужный префикс
```

### Добавить фильтры элементов:

```javascript
// В popup.js, строка 24
const elements = document.querySelectorAll('*:not(script):not(style):not(noscript)');
// Добавьте :not(header):not(footer) и т.д.
```

## 🚀 Расширение функционала

### Добавить MutationObserver:

```javascript
const observer = new MutationObserver((mutations) => {
  mutations.forEach(mutation => {
    mutation.addedNodes.forEach(node => {
      if (node.nodeType === 1 && !node.hasAttribute('data-component-id')) {
        node.setAttribute('data-component-id', `COMP_${counter++}`);
      }
    });
  });
});

observer.observe(document.body, { childList: true, subtree: true });
```

### Добавить визуальный Inspector:

```javascript
document.addEventListener('mouseover', (e) => {
  e.target.style.outline = '2px solid red';
  showTooltip(e.target.getAttribute('data-component-id'));
});
```

## 📝 Changelog

### v4.0.0 (2024-01-15) - 🎉 Полный Inspector из src!
- ✅ **NEW:** Полный функционал Inspector из src/modules/Inspector.js
- ✅ **NEW:** 3 вкладки - Элемент / Трассировка / Граф
- ✅ **NEW:** Экспорт отчётов (JSON/Markdown/HTML)
- ✅ **NEW:** Граф зависимостей с проверкой циклов
- ✅ **NEW:** Mermaid диаграммы в отчётах
- ✅ **NEW:** Статистика по модулям и функциям
- ✅ **NEW:** Автозагрузка dependencies_map.json
- ✅ **NEW:** Построение графа из DOM

### v3.2.0 (2024-01-15)
- ✅ Автоматическое добавление FUNC_ID
- ✅ Парсинг JS кода
- ✅ Runtime свойства `__funcId`
- ✅ Два счётчика

### v3.1.0 (2024-01-15)
- ✅ Первый релиз
- ✅ Базовая функциональность
- ✅ Поддержка iframe
- ✅ postMessage для cross-origin

## 📄 Лицензия

MIT License - используйте свободно!

## 📊 Inspector API

### Основные функции (24 функции)

1. **init()** - Инициализация Inspector
2. **createPanel()** - Создание панели с 3 вкладками
3. **createOverlay()** - Создание подсветки элементов
4. **attachKeyboardShortcut()** - Горячая клавиша F2
5. **activate()** - Активация Inspector
6. **deactivate()** - Деактивация Inspector
7. **handleMouseOver()** - Обработка наведения мыши
8. **handleClick()** - Обработка клика (закрепление)
9. **togglePin()** - Переключение закрепления
10. **pinElement()** - Закрепление элемента
11. **showInfo()** - Отображение информации об элементе
12. **copyToClipboard()** - Копирование ID в буфер
13. **toggleLiveTrace()** - Вкл/выкл живой трассировки
14. **switchTab()** - Переключение вкладок
15. **updateTraceView()** - Обновление трассировки
16. **updateGraphView()** - Обновление графа зависимостей
17. **exportReport()** - Экспорт отчёта (JSON/MD/HTML)
18. **generateMarkdownReport()** - Генерация Markdown
19. **generateHTMLReport()** - Генерация HTML
20. **generateMermaidDiagram()** - Генерация Mermaid
21. **loadDependencyGraph()** - Загрузка графа
22. **buildGraphFromDOM()** - Построение графа из DOM
23. **calculateGraphStats()** - Статистика графа
24. **checkCycles()** - Проверка циклов (DFS)

### Использование

```javascript
// Активация
Inspector.activate();

// Или горячая клавиша
F2

// Или кнопка в popup
"🔍 Activate Inspector (F2)"

// Экспорт отчёта
Inspector.exportReport();

// Проверка циклов
const cycles = Inspector.checkCycles();
console.log('Циклы:', cycles);
```

## 🤝 Вклад

Создано для PromAi архитектуры. Следует стандартам:
- `data-component-id="COMP_XXXXX"` - Элементы
- `data-module-id="M_AUTO_1.0"` - Модули
- `functionName.__funcId = "FUNC_name_XXX"` - Функции
- Уникальные ID для всех элементов и функций

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте консоль DevTools (F12)
2. Убедитесь что расширение активно
3. Попробуйте перезагрузить страницу
4. Проверьте права доступа в chrome://extensions/

---

**Готово к использованию! 🎉**
