# function_registry.json - Полное руководство

## 📋 Обзор

function_registry.json - это реестр всех функций проекта с полной трассировкой вызовов. Он документирует:
- Какая функция вызывает другую (calls)
- Кто вызывает функцию (calledBy)
- Параметры и возвращаемые значения
- Сложность функций
- Статистику использования

**Версия:** 2.6.0  
**Покрытие:** 30 функций из 180 (16.7%)  
**Файл:** [function_registry.json](../function_registry.json)

---

## 🎯 Зачем нужно

### Проблемы которые решает:

#### 1. Невозможно понять поток данных
**Было:**
```javascript
// В DataManager.js
addCategory() {
    this.saveToLocalStorage();           // ← Кто знает об этом вызове?
    window.uiManager.hideModals();       // ← Скрытая зависимость
    window.accordionManager.renderAccordion(); // ← Неочевидная связь
}
```

**Стало:**
```json
{
  "FUNC_addCategory_004": {
    "calls": [
      "FUNC_saveToLocalStorage_002",
      "FUNC_hideModals_002",
      "FUNC_renderAccordion_002"
    ]
  }
}
```

#### 2. Сложно отлаживать баги
**Проблема:** Баг в renderAccordion, но непонятно откуда он вызывается

**Решение:** Смотрим calledBy и видим все источники вызова

#### 3. Неизвестны узкие места
**Проблема:** Приложение тормозит, но непонятно где

**Решение:** Смотрим mostCalledFunction и mostComplexFunction

#### 4. Мёртвый код
**Проблема:** Есть функции которые никто не вызывает

**Решение:** Смотрим unusedFunctions

---

## 🔧 Как работает

### Структура файла

```json
{
  "projectId": "PROJECT_TechKnowledgeBase",
  "version": "2.6.0",
  "description": "Реестр всех функций с трассировкой вызовов",
  
  "functions": {
    "FUNC_ID": { ... }
  },
  
  "stats": {
    "totalFunctions": 180,
    "mostCalledFunction": "FUNC_saveToLocalStorage_002"
  },
  
  "callGraph": {
    "hotPaths": [ ... ]
  },
  
  "recommendations": [ ... ]
}
```

### Структура функции

```json
{
  "FUNC_addCategory_004": {
    "module": "MODULE_DataManager_VER_1.0",
    "name": "addCategory",
    "params": [],
    "returns": "void",
    "calls": [
      "FUNC_saveToLocalStorage_002",
      "FUNC_hideModals_002",
      "FUNC_renderAccordion_002",
      "FUNC_showNotification_003",
      "FUNC_scheduleAutoSave_018"
    ],
    "calledBy": ["USER_ACTION"],
    "complexity": 5,
    "lineStart": 30,
    "lineEnd": 48
  }
}
```

**Поля:**
- `module` - к какому модулю относится
- `name` - имя функции в коде
- `params` - параметры функции
- `returns` - что возвращает
- `calls` - кого вызывает эта функция
- `calledBy` - кто вызывает эту функцию
- `complexity` - сложность (1-20, рекомендуется <10)
- `lineStart/lineEnd` - строки в файле

---

## 💻 Использование

### Базовое использование

#### 1. Найти функцию
```json
// Открыть function_registry.json
// Ctrl+F → "FUNC_addCategory_004"
```

#### 2. Посмотреть что вызывает
```json
{
  "calls": [
    "FUNC_saveToLocalStorage_002",
    "FUNC_hideModals_002",
    "FUNC_renderAccordion_002"
  ]
}
```

#### 3. Посмотреть кто вызывает
```json
{
  "calledBy": ["USER_ACTION", "FUNC_importFromJSON_010"]
}
```

### Продвинутое использование

#### Построить полную цепочку вызовов

**Задача:** Понять что происходит при добавлении категории

**Решение:**
```
1. Найти FUNC_addCategory_004
2. Посмотреть calls:
   - FUNC_saveToLocalStorage_002
   - FUNC_hideModals_002
   - FUNC_renderAccordion_002
   - FUNC_showNotification_003
   - FUNC_scheduleAutoSave_018

3. Для каждой функции посмотреть её calls:
   FUNC_renderAccordion_002 → calls:
     - FUNC_buildAccordion_004

4. Построить дерево:
   USER_ACTION
     → FUNC_addCategory_004 (5ms)
       ├─ FUNC_saveToLocalStorage_002 (2ms)
       ├─ FUNC_hideModals_002 (1ms)
       ├─ FUNC_renderAccordion_002 (15ms)
       │  └─ FUNC_buildAccordion_004 (12ms)
       ├─ FUNC_showNotification_003 (1ms)
       └─ FUNC_scheduleAutoSave_018 (1ms)
```

#### Найти узкие места

**Задача:** Оптимизировать производительность

**Решение:**
```json
// Смотрим stats
{
  "stats": {
    "mostCalledFunction": "FUNC_saveToLocalStorage_002",
    "mostCalledCount": 13,
    "mostComplexFunction": "FUNC_buildAccordion_004",
    "mostComplexScore": 15
  }
}
```

**Выводы:**
- FUNC_saveToLocalStorage_002 вызывается 13 раз → централизовать
- FUNC_buildAccordion_004 сложность 15 → разбить на части

#### Найти неиспользуемые функции

**Задача:** Очистить код

**Решение:**
```json
// Смотрим stats.unusedFunctions
{
  "stats": {
    "unusedFunctions": []
  }
}
```

Если пусто - все функции используются ✅

---

## 📊 API Reference

### Секция functions

**Описание:** Словарь всех функций проекта

**Ключ:** FUNC_ID (например, FUNC_addCategory_004)

**Значение:** Объект с полями:
- `module` (string) - MODULE_ID
- `name` (string) - имя функции
- `params` (array) - параметры
- `returns` (string) - тип возврата
- `calls` (array) - список FUNC_ID которые вызывает
- `calledBy` (array) - список FUNC_ID которые вызывают
- `complexity` (number) - сложность 1-20
- `lineStart` (number) - начальная строка
- `lineEnd` (number) - конечная строка
- `note` (string, optional) - примечание

### Секция stats

**Описание:** Статистика по всем функциям

**Поля:**
- `totalFunctions` (number) - всего функций в проекте
- `analyzedFunctions` (number) - проанализировано
- `coveragePercent` (number) - процент покрытия
- `mostCalledFunction` (string) - самая вызываемая
- `mostCalledCount` (number) - сколько раз
- `mostComplexFunction` (string) - самая сложная
- `mostComplexScore` (number) - сложность
- `recursiveFunctions` (array) - рекурсивные функции
- `unusedFunctions` (array) - неиспользуемые

### Секция callGraph

**Описание:** Граф вызовов и hot paths

**Поля:**
- `description` (string) - описание
- `hotPaths` (array) - критические пути выполнения

**Hot Path:**
```json
{
  "name": "Add Category Flow",
  "path": ["USER_ACTION", "FUNC_addCategory_004", "..."],
  "avgDuration": "~20ms"
}
```

### Секция recommendations

**Описание:** Рекомендации по улучшению

**Формат:**
```json
{
  "type": "performance|architecture|complexity",
  "priority": "high|medium|low",
  "issue": "описание проблемы",
  "solution": "предлагаемое решение"
}
```

---

## 🎓 Примеры

### Пример 1: Отладка бага "Данные не сохраняются"

**Проблема:** После добавления категории данные не сохраняются в LocalStorage

**Шаги:**
1. Открыть function_registry.json
2. Найти FUNC_addCategory_004
3. Проверить calls - есть ли FUNC_saveToLocalStorage_002?
4. Если есть - проблема в другом месте
5. Если нет - добавить вызов saveToLocalStorage

**Результат:**
```json
{
  "FUNC_addCategory_004": {
    "calls": [
      "FUNC_saveToLocalStorage_002"  // ← Есть!
    ]
  }
}
```
Вызов есть, значит проблема в самой функции saveToLocalStorage.

### Пример 2: Оптимизация производительности

**Проблема:** Приложение тормозит при раскрытии категорий

**Шаги:**
1. Открыть function_registry.json
2. Найти stats.mostCalledFunction
3. Найти stats.mostComplexFunction
4. Проверить recommendations

**Результат:**
```json
{
  "stats": {
    "mostCalledFunction": "FUNC_saveToLocalStorage_002",
    "mostCalledCount": 13,
    "mostComplexFunction": "FUNC_buildAccordion_004",
    "mostComplexScore": 15
  },
  "recommendations": [
    {
      "type": "performance",
      "issue": "FUNC_buildAccordion_004 вызывается слишком часто",
      "solution": "Добавить debounce для renderAccordion"
    }
  ]
}
```

**Действия:**
- Добавить debounce для renderAccordion (300ms)
- Разбить buildAccordion на мелкие функции
- Централизовать saveToLocalStorage через event bus

### Пример 3: Понимание архитектуры

**Задача:** Новый разработчик хочет понять как работает добавление технологии

**Шаги:**
1. Найти FUNC_addTechnology_005
2. Построить дерево вызовов
3. Изучить каждую функцию

**Результат:**
```
USER_ACTION (клик на кнопку "+")
  ↓
FUNC_addTechnology_005
  ├─ FUNC_getNodeByPath_001 (найти родителя)
  ├─ FUNC_saveToLocalStorage_002 (сохранить)
  ├─ FUNC_hideModals_002 (закрыть модалку)
  ├─ FUNC_renderAccordion_002 (перерисовать)
  │  └─ FUNC_buildAccordion_004 (построить дерево)
  ├─ FUNC_showNotification_003 (показать уведомление)
  └─ FUNC_scheduleAutoSave_018 (запланировать автосохранение)
```

Теперь понятен весь поток!

---

## 🔍 Troubleshooting

### Проблема: Функция не найдена в реестре

**Симптом:** Ищу FUNC_myFunction_001, но её нет в function_registry.json

**Причина:** Покрытие 16.7% - не все функции проанализированы

**Решение:**
1. Проверить что функция существует в коде
2. Добавить вручную в function_registry.json
3. Или дождаться полного анализа (180 функций)

### Проблема: Вызовы не совпадают с кодом

**Симптом:** В реестре написано что функция вызывает X, но в коде она вызывает Y

**Причина:** Реестр устарел после изменений в коде

**Решение:**
1. Обновить function_registry.json вручную
2. Или запустить автоматический парсер (когда будет создан)

### Проблема: Сложность функции кажется неправильной

**Симптом:** Простая функция имеет сложность 10

**Причина:** Сложность рассчитывается по количеству вызовов, условий, циклов

**Решение:**
1. Проверить алгоритм расчёта сложности
2. Возможно функция действительно сложная (много вызовов)

### Проблема: calledBy пустой, но функция используется

**Симптом:** calledBy: [], но функция точно вызывается

**Причина:** Функция вызывается из HTML (onclick) или не проанализирована

**Решение:**
1. Добавить "USER_ACTION" в calledBy если вызов из HTML
2. Проверить что все вызовы задокументированы

---

## 📚 См. также

- [PROMPT_FunctionRegistry.md](../.aws/amazonq/prompts/PROMPT_FunctionRegistry.md) - промпт для работы
- [IMPROVEMENT_PLAN_ANALYZED.md](../IMPROVEMENT_PLAN_ANALYZED.md) - план улучшений
- [PHASE1_PROGRESS.md](../PHASE1_PROGRESS.md) - прогресс реализации
- [project_registry.json](../project_registry.json) - реестр модулей
- [dependencies_map.json](../dependencies_map.json) - зависимости модулей

---

## 🚀 Следующие шаги

После освоения function_registry.json:
1. Изучить [DOMFactory](DOMFactory_GUIDE.md) - автогенерация data-атрибутов
2. Изучить [architecture_layers.json](ArchitectureLayers_GUIDE.md) - слои архитектуры
3. Изучить [Inspector Live Trace](InspectorLiveTrace_GUIDE.md) - живая трассировка

---

**Версия документа:** 1.0  
**Дата:** 2024-01-15  
**Автор:** PromAi System
