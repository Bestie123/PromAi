# Daily Development

Начинаем работу над: {задача}

## Параметры
- **Сегодняшняя задача:** {TODAY_TASK}
- **Текущий прогресс:** {что уже сделано}
- **Блокеры:** {проблемы если есть}
- **Приоритет:** {high/medium/low}

## Шаг 1: Загрузить состояние

### Из project_registry.json
```javascript
// Загрузить информацию о проекте
const project = projectRegistry;
// - projectId: ID проекта
// - modules: список модулей
// - stats: статистика проекта
```

### Из todo.json
```javascript
// Загрузить активные задачи
const activeTasks = todo.tasks.filter(t => t.status === 'inProgress');
const backlog = todo.tasks.filter(t => t.status === 'backlog');

console.log('Активные задачи:', activeTasks.length);
console.log('В бэклоге:', backlog.length);
```

### Из changelog.md
```javascript
// Проверить последние изменения
// Что было сделано вчера?
// Какие модули затронуты?
```

## Шаг 2: Определить следующие шаги

### Конкретные шаги с модулями
1. **{Шаг 1}** - {описание}
   - Модули: {MODULE_ID_1, MODULE_ID_2}
   - Время: {оценка в часах}
   - Зависимости: {что нужно сделать сначала}

2. **{Шаг 2}** - {описание}
   - Модули: {MODULE_ID_3}
   - Время: {оценка в часах}
   - Зависимости: {Шаг 1}

3. **{Шаг 3}** - {описание}
   - Модули: {MODULE_ID_4}
   - Время: {оценка в часах}
   - Зависимости: нет (можно параллельно)

### Добавить data-атрибуты для Inspector
```javascript
// Для всех новых элементов
const element = domFactory.create({
    moduleId: 'MODULE_{Name}_VER_1.0',
    componentId: 'COMP_{Name}',
    functionId: 'FUNC_{name}_{number}'
});
```

### Интегрировать с существующими модулями
```javascript
// Проверить зависимости
const deps = dependenciesMap.edges.filter(e => e.from === moduleId);

// Обновить зависимости если нужно
module.dependencies.push('MODULE_NewDep_VER_1.0');
```

### Создать тесты
```javascript
// Для каждой новой функции
describe('FUNC_{name}_{number}', () => {
    it('должен работать корректно', () => {
        // тест
    });
});
```

## Шаг 3: Вопросы по архитектуре

### Организация модуля
- В какой слой поместить модуль? (LAYER_1_CORE / LAYER_2_DATA / LAYER_3_UI / LAYER_4_INTEGRATION / LAYER_5_KNOWLEDGE)
- Какие зависимости нужны?
- Нужно ли разбить на несколько модулей?

### События и коммуникация
- Какие события должны эмитироваться?
- Кто должен подписываться на события?
- Нужен ли EventBus?

### Интеграция
- Как интегрировать с DataManager?
- Нужна ли интеграция с UIManager?
- Как обрабатывать ошибки?

## Шаг 4: Ожидаемый результат

### Чистый код с уникальными ID
```javascript
// MODULE_{Name}_VER_1.0
const moduleName = {
    moduleId: 'MODULE_{Name}_VER_1.0',
    version: '1.0',
    dependencies: [],
    
    // FUNC_{name}_001 - Описание
    functionName() {
        // Реализация
    }
};

window.moduleName = moduleName;
```

### Рабочие примеры использования
```javascript
// Пример 1: Базовое использование
moduleName.functionName();

// Пример 2: С параметрами
moduleName.functionName(param1, param2);

// Пример 3: Интеграция с другими модулями
dataManager.saveData();
moduleName.functionName();
```

### Интеграция с главным приложением
```javascript
// В main.js
document.addEventListener('DOMContentLoaded', function() {
    // Инициализация нового модуля
    if (window.moduleName) {
        window.moduleName.init();
    }
});
```

### Обновленная документация
```markdown
# {Module Name}

## Что добавлено
- FUNC_{name}_001 - {описание}
- FUNC_{name}_002 - {описание}

## Как использовать
\`\`\`javascript
moduleName.functionName();
\`\`\`

## Зависимости
- MODULE_Dependency_VER_1.0
```

## Шаг 5: Обновить state файлы

### 1. project_registry.json
```json
{
  "modules": {
    "MODULE_{Name}_VER_1.0": {
      "path": "src/modules/{Name}.js",
      "functions": ["FUNC_{name}_001", "FUNC_{name}_002"],
      "dependencies": [],
      "status": "active",
      "layer": "LAYER_X_XXX"
    }
  },
  "stats": {
    "totalModules": 12,
    "totalFunctions": 220
  }
}
```

### 2. dependencies_map.json
```json
{
  "nodes": [
    {"id": "MODULE_{Name}_VER_1.0", "layer": "LAYER_X_XXX"}
  ],
  "edges": [
    {"from": "MODULE_{Name}_VER_1.0", "to": "MODULE_Dependency_VER_1.0"}
  ]
}
```

### 3. todo.json
```json
{
  "tasks": [
    {
      "id": "TASK_XXX",
      "title": "{Сегодняшняя задача}",
      "status": "done",
      "completedDate": "2024-01-15"
    },
    {
      "id": "TASK_YYY",
      "title": "Создать тесты для MODULE_{Name}_VER_1.0",
      "status": "inProgress"
    }
  ]
}
```

### 4. changelog.md
```markdown
## [1.X.X] - 2024-01-15
### Added
- MODULE_{Name}_VER_1.0 - {описание}
- FUNC_{name}_001, FUNC_{name}_002

### Progress
- Завершена задача TASK_XXX
- В работе: TASK_YYY
```

## Примеры использования

### Пример 1: Добавление нового модуля
```
Сегодняшняя задача: Создать модуль уведомлений
Текущий прогресс: Создана структура проекта
Блокеры: Нет

Следующие шаги:
1. Создать MODULE_NotificationManager_VER_1.0 (2 часа)
   - FUNC_show_001 - Показать уведомление
   - FUNC_hide_002 - Скрыть уведомление
   - FUNC_queue_003 - Очередь уведомлений

2. Интегрировать с UIManager (1 час)
   - Использовать DOMFactory для создания элементов
   - Добавить data-атрибуты

3. Создать тесты (1 час)
   - Unit тесты для каждой функции
   - Integration тесты с UIManager
```

### Пример 2: Исправление бага
```
Сегодняшняя задача: Исправить утечку памяти в Inspector
Текущий прогресс: Баг воспроизведен
Блокеры: Нет

Следующие шаги:
1. Анализ проблемы (30 минут)
   - Использовать FunctionTracer
   - Найти где не очищаются event listeners

2. Исправление (1 час)
   - Сохранить bound handlers
   - Очистить в deactivate()
   - Обновить версию 1.0 → 1.0.1

3. Регресс-тесты (1 час)
   - Тест на утечку памяти
   - Проверить все зависимые модули
```

### Пример 3: Рефакторинг
```
Сегодняшняя задача: Разделить большую функцию saveData
Текущий прогресс: Функция идентифицирована (150 строк)
Блокеры: Нет

Следующие шаги:
1. Анализ функции (30 минут)
   - Определить логические блоки
   - Найти зависимые модули

2. Разделение (2 часа)
   - validateData() - валидация
   - prepareData() - подготовка
   - persistData() - сохранение

3. Обновление тестов (1 час)
   - Тесты для каждой новой функции
   - Проверить зависимые модули
```

## Проверка работы

### ✅ Чеклист
- [ ] Задача из todo.json обновлена
- [ ] Код написан с FUNC_ID
- [ ] Используется DOMFactory для элементов
- [ ] Добавлены data-атрибуты
- [ ] Созданы тесты
- [ ] project_registry.json обновлён
- [ ] dependencies_map.json обновлён
- [ ] todo.json обновлён
- [ ] changelog.md обновлён
- [ ] Документация обновлена

### Консоль должна показать:
```
[MODULE_{Name}_VER_1.0] Initialized
// Нет ошибок
// Все функции работают
```

### Inspector должен показать:
```
// При наведении на элементы
MODULE_ID: MODULE_{Name}_VER_1.0
COMPONENT_ID: COMP_{Name}
FUNCTION_ID: FUNC_{name}_001
```

## Следующие действия

### Зависят от задачи
- Если создан модуль → `@PROMPT_create-tests`
- Если исправлен баг → `@PROMPT_code-review`
- Если рефакторинг → `@PROMPT_create-tests` + `@PROMPT_code-review`
- Если добавлена функция → `@PROMPT_create-tests`

### Обновить todo.json
```json
{
  "tasks": [
    {
      "id": "TASK_XXX",
      "title": "{Завершённая задача}",
      "status": "done",
      "completedDate": "2024-01-15"
    },
    {
      "id": "TASK_YYY",
      "title": "{Следующая задача}",
      "status": "inProgress",
      "startedDate": "2024-01-15"
    }
  ]
}
```

### Записать прогресс в changelog.md
```markdown
## [1.X.X] - 2024-01-15
### Progress
- ✅ Завершена задача: {описание}
- 🔄 В работе: {описание}
- 📝 Создано: MODULE_{Name}_VER_1.0
- 🧪 Тесты: {покрытие}%
```

## Troubleshooting

### Проблема: Не знаю с чего начать
**Решение:** Проверить todo.json
```javascript
// Найти задачи с высоким приоритетом
const highPriority = todo.tasks.filter(t => 
    t.priority === 'high' && t.status === 'backlog'
);
console.log('Начать с:', highPriority[0]);
```

### Проблема: Блокер - зависимость не готова
**Решение:** Переключиться на другую задачу
```javascript
// Найти задачи без зависимостей
const independent = todo.tasks.filter(t => 
    !t.dependencies && t.status === 'backlog'
);
```

### Проблема: Не уверен в архитектуре
**Решение:** Проверить существующие модули
```javascript
// Посмотреть похожие модули
const similar = Object.keys(projectRegistry.modules)
    .filter(m => m.includes('Manager'));
console.log('Похожие модули:', similar);
```

## См. также
- [create-module.md](create-module.md) - Создание модуля
- [add-function.md](add-function.md) - Добавление функции
- [create-tests.md](create-tests.md) - Создание тестов
- [code-review.md](code-review.md) - Код-ревью
- [project-standards.md](../project-standards.md) - Стандарты PromAi
