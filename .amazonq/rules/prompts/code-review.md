# Code Review

Выполни код-ревью для: {укажи модуль/файл}

## Контекст
- Модуль ID: {MODULE_ID}
- Файл: {путь к файлу}
- Слой: {LAYER_X_XXX}

## Загрузить контекст

### 1. Модуль из project_registry.json
```javascript
// Загрузить информацию о модуле
const module = projectRegistry.modules[moduleId];
// - path: путь к файлу
// - functions: список FUNC_ID
// - dependencies: зависимости
// - status: статус модуля
// - layer: архитектурный слой
```

### 2. Зависимости из dependencies_map.json
```javascript
// Проверить граф зависимостей
const dependencies = dependenciesMap.edges.filter(e => e.from === moduleId);
const dependents = dependenciesMap.edges.filter(e => e.to === moduleId);
```

### 3. Слой из architecture_layers.json
```javascript
// Проверить правила слоя
const layer = architectureLayers.layers[layerId];
// - canDependOn: разрешённые зависимости
// - rules: правила слоя
```

## Критерии проверки

### 1. Архитектура (SOLID принципы)
- ✅ **Single Responsibility:** Модуль отвечает за одну область
- ✅ **Open/Closed:** Расширяемый без изменения существующего кода
- ✅ **Liskov Substitution:** Не применимо (нет наследования)
- ✅ **Interface Segregation:** Чёткие публичные методы
- ✅ **Dependency Inversion:** Зависимости через window объект

### 2. Структура модуля
```javascript
// Проверить наличие:
const module = {
    moduleId: 'MODULE_Name_VER_1.0',  // ✅ Обязательно
    version: '1.0',                    // ✅ Обязательно
    dependencies: [],                  // ✅ Обязательно
    
    // FUNC_functionName_001           // ✅ Комментарий с FUNC_ID
    functionName() {                   // ✅ Реализация
        // ...
    }
};

window.moduleName = module;            // ✅ Экспорт на window
```

### 3. Data-атрибуты для Inspector
```javascript
// Все DOM элементы должны иметь:
const element = domFactory.create({
    moduleId: 'MODULE_Name_VER_1.0',      // ✅ Обязательно
    componentId: 'COMP_ComponentName',    // ✅ Обязательно
    functionId: 'FUNC_functionName_001'   // ✅ Для интерактивных элементов
});

// ❌ НЕ использовать document.createElement напрямую
// ❌ НЕ забывать data-атрибуты
```

### 4. Зависимости
- ✅ Все зависимости объявлены в dependencies[]
- ✅ Нет циклических зависимостей
- ✅ Соблюдены правила слоёв
- ✅ Зависимости загружены до модуля (порядок в index.html)

### 5. Качество кода
- ✅ Читаемые имена переменных и функций
- ✅ Нет дублирования кода (DRY)
- ✅ Минимальная сложность функций
- ✅ Комментарии только для FUNC_ID
- ✅ Единый стиль кодирования

### 6. Производительность
- ✅ Нет утечек памяти (event listeners очищаются)
- ✅ Нет избыточных DOM операций
- ✅ Оптимальные алгоритмы
- ✅ Debouncing для частых операций

### 7. Безопасность
- ✅ Нет XSS уязвимостей (innerHTML с пользовательским вводом)
- ✅ Валидация входных данных
- ✅ Безопасное хранение токенов
- ✅ Нет eval() или Function()

### 8. Тестируемость
- ✅ Функции можно тестировать изолированно
- ✅ Нет глобального состояния (кроме window.techData)
- ✅ Чёткие входы и выходы функций

## Конкретные вопросы

### Архитектура
- Соответствует ли модуль своему слою?
- Правильно ли выбраны зависимости?
- Нет ли нарушений правил слоёв?
- Можно ли разбить модуль на несколько?

### Код
- Есть ли дублирование кода?
- Можно ли упростить сложные функции?
- Правильно ли используется DOMFactory?
- Все ли FUNC_ID уникальны?

### Производительность
- Есть ли утечки памяти?
- Оптимальны ли DOM операции?
- Нужен ли debouncing/throttling?
- Можно ли кэшировать результаты?

### Безопасность
- Есть ли XSS уязвимости?
- Валидируются ли входные данные?
- Безопасно ли хранятся токены?

## Ожидаемый результат

### 1. Конструктивная критика
```markdown
## Найденные проблемы

### Критичные (исправить немедленно)
- ❌ Циклическая зависимость с MODULE_X
- ❌ XSS уязвимость в функции FUNC_render_003
- ❌ Утечка памяти: event listener не очищается

### Важные (исправить в ближайшее время)
- ⚠️ Нарушение SRP: модуль делает слишком много
- ⚠️ Отсутствуют data-атрибуты в 3 элементах
- ⚠️ Дублирование кода в FUNC_save_002 и FUNC_update_005

### Рекомендации (улучшения)
- 💡 Можно использовать debouncing для автосохранения
- 💡 Рекомендуется разбить на 2 модуля
- 💡 Добавить валидацию входных данных
```

### 2. Предложения по улучшению
```javascript
// ❌ Было (проблема)
function render() {
    const div = document.createElement('div');
    div.innerHTML = userInput; // XSS!
    document.body.appendChild(div);
}

// ✅ Стало (решение)
function render() {
    const div = domFactory.create({
        moduleId: this.moduleId,
        componentId: 'COMP_Container',
        functionId: 'FUNC_render_001',
        textContent: userInput // Безопасно
    });
    document.body.appendChild(div);
}
```

### 3. Проверка зависимостей
```javascript
// Проверить циклы
const cycles = dependencyValidator.findCycles();
if (cycles.length > 0) {
    console.error('❌ Циклические зависимости:', cycles);
}

// Проверить правила слоёв
const violations = dependencyValidator.checkLayerViolations();
if (violations.length > 0) {
    console.error('❌ Нарушения слоёв:', violations);
}
```

### 4. Оценка качества
```markdown
## Оценка модуля

**Архитектура:** 8/10
- ✅ Соответствует слою
- ✅ Правильные зависимости
- ⚠️ Можно разбить на 2 модуля

**Код:** 7/10
- ✅ Читаемый код
- ⚠️ Есть дублирование
- ❌ Отсутствуют data-атрибуты

**Производительность:** 9/10
- ✅ Нет утечек памяти
- ✅ Оптимальные алгоритмы
- 💡 Можно добавить кэширование

**Безопасность:** 6/10
- ❌ XSS уязвимость
- ⚠️ Нет валидации входных данных
- ✅ Токены хранятся безопасно

**Общая оценка:** 7.5/10
```

## Обновить файлы

### 1. project_registry.json
```json
{
  "modules": {
    "MODULE_Name_VER_1.0": {
      "status": "needs_review",  // или "reviewed", "needs_fixes"
      "reviewDate": "2024-01-15",
      "reviewScore": 7.5
    }
  }
}
```

### 2. dependencies_map.json
```javascript
// Проверить целостность связей
// Убедиться что все зависимости корректны
// Нет ли циклов
```

### 3. architecture_layers.json
```javascript
// Проверить что модуль в правильном слое
// Проверить что зависимости соответствуют правилам
```

### 4. todo.json
```json
{
  "tasks": [
    {
      "id": "TASK_XXX",
      "title": "Исправить XSS уязвимость в MODULE_Name_VER_1.0",
      "priority": "critical",
      "status": "backlog",
      "assignedTo": "MODULE_Name_VER_1.0",
      "created": "2024-01-15"
    },
    {
      "id": "TASK_YYY",
      "title": "Добавить data-атрибуты в MODULE_Name_VER_1.0",
      "priority": "high",
      "status": "backlog"
    }
  ]
}
```

### 5. changelog.md
```markdown
## [1.X.X] - 2024-01-15
### Reviewed
- MODULE_Name_VER_1.0 - Code review completed (score: 7.5/10)
  - Found: XSS vulnerability, missing data-attributes
  - Recommended: Split into 2 modules, add validation
```

## Примеры использования

### Пример 1: Ревью модуля LAYER_2_DATA
```
Модуль ID: MODULE_DataManager_VER_1.0
Файл: src/modules/DataManager.js
Слой: LAYER_2_DATA

Проверить:
- ✅ Не зависит от UI слоёв
- ✅ Только CRUD операции
- ✅ Работа с LocalStorage
- ⚠️ Можно добавить валидацию данных
```

### Пример 2: Ревью модуля LAYER_3_UI
```
Модуль ID: MODULE_AccordionManager_VER_1.0
Файл: src/modules/AccordionManager.js
Слой: LAYER_3_UI

Проверить:
- ✅ Использует DOMFactory
- ✅ Все элементы имеют data-атрибуты
- ✅ Зависит только от CORE и DATA
- ⚠️ Можно оптимизировать рендеринг
```

### Пример 3: Ревью с Inspector
```
1. Активировать Inspector (F2)
2. Открыть модуль в редакторе
3. Проверить каждую функцию:
   - Навести на созданные элементы
   - Проверить наличие data-атрибутов
   - Проверить MODULE_ID, COMPONENT_ID, FUNCTION_ID
4. Переключиться на вкладку "Граф"
5. Проверить зависимости модуля
6. Экспортировать отчёт (💾 → Markdown)
```

## Чеклист ревью

### Структура
- [ ] Есть moduleId, version, dependencies
- [ ] Все функции имеют FUNC_ID комментарии
- [ ] Модуль экспортирован на window
- [ ] Файл в правильной директории (src/modules/)

### Архитектура
- [ ] Модуль в правильном слое
- [ ] Зависимости соответствуют правилам слоёв
- [ ] Нет циклических зависимостей
- [ ] Соблюдён SRP (Single Responsibility)

### Код
- [ ] Используется DOMFactory для создания элементов
- [ ] Все элементы имеют data-атрибуты
- [ ] Нет дублирования кода
- [ ] Читаемые имена переменных

### Производительность
- [ ] Event listeners очищаются
- [ ] Нет избыточных DOM операций
- [ ] Используется debouncing где нужно

### Безопасность
- [ ] Нет XSS уязвимостей
- [ ] Валидация входных данных
- [ ] Безопасное хранение токенов

### Тестируемость
- [ ] Функции можно тестировать изолированно
- [ ] Нет глобального состояния
- [ ] Чёткие входы и выходы

## Troubleshooting

### Проблема: Не могу найти модуль в registry
**Решение:** Проверить project_registry.json
```javascript
console.log(Object.keys(projectRegistry.modules));
```

### Проблема: Циклические зависимости
**Решение:** Использовать DependencyValidator
```javascript
const cycles = window.dependencyValidator.findCycles();
console.log(cycles);
```

### Проблема: Нарушения правил слоёв
**Решение:** Проверить architecture_layers.json
```javascript
const violations = window.dependencyValidator.checkLayerViolations();
console.log(violations);
```

## См. также
- [project-standards.md](../project-standards.md) - Стандарты PromAi
- [architecture_layers.json](../../architecture_layers.json) - Правила слоёв
- [Inspector_GUIDE.md](../../docs/guides/Inspector_GUIDE.md) - Отладка с Inspector
- [DOMFactory_GUIDE.md](../../docs/guides/DOMFactory_GUIDE.md) - Создание элементов
